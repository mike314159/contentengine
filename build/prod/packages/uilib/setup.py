from setuptools import setup, find_packages

setup(
   name='uilib',
   version='1.0',
   description='Useful UI Classes',
   author='Man Foo',
   author_email='foomail@foo.com',
   packages=find_packages(),
   install_requires=[],
)
